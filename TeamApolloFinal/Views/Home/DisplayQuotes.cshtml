@model IEnumerable<TeamApolloFinal.Models.Apollo>
@{
    ViewBag.Title = "DisplayQuotes";
}

<h2>DisplayQuotes</h2>

<body>
    <div>
        @foreach(var quote in Model)
        {
            <p>@quote.QuoteID ) @quote.Motivation</p>

            @Html.ActionLink("Home |", "Index")
            @Html.ActionLink(" Add Quote |", "AddQuote")
            @Html.ActionLink(" Delete Quote", "DeleteQuote", new { id = quote.QuoteID })
        }
    </div>


</body>
