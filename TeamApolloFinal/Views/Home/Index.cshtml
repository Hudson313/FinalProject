@model IEnumerable<TeamApolloFinal.Models.Apollo>
@using TeamApolloFinal.Models
@{
    Layout = null;
}
@*Hello*@
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    @Styles.Render("~/Content/css")
    <style>
        #owl-demo .item {
            margin: 3px;
        }

            #owl-demo .item img {
                display: block;
                width: 100%;
                height: auto;
            }
    </style>
    <!-- Important Owl stylesheet -->

    <link href="~/Content/OwlCarousel/owl.carousel.css" rel="stylesheet" />


    <!-- Default Theme -->

    <link href="~/Content/OwlCarousel/owl.theme.css" rel="stylesheet" />

    <!--  jQuery 1.7+  -->

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <!-- Include js plugin -->

    <script src="~/Scripts/owl.carousel.min.js"></script>

    <script>
        var quotesArry = [];

        @foreach(var quote in Model) {
            <text>
        quotesArry.push(`@quote.Motivation`);
        </text>
        }



        function display() {
            //document.getElementById("quoteDiv").innerHTML = quotes[0];
            document.getElementById("quoteDiv").innerHTML = quotesArry[Math.floor((Math.random() * (quotesArry.length + 2)))];
        }
    </script>
</head>

<body style="background-image: url('../../Content/Images/Space.jpg'); height: 100%; width: auto;">
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("AllSystemsGo", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("AdminLogin", "LogIn")</li>
                    <li>@Html.ActionLink("Register", "Register")</li>
                </ul>
                @*@Html.Partial("_LoginPartial")*@
            </div>
        </div>
    </div>
    @*@*Quotes*@
    
    <button onclick="display()">Fuel Up! Click Here For Another Quote</button>
    <div id="quoteDiv" style="font-family:'Gentium Book Basic'; font-weight:bold; font-size:20px; color:white; ">@ViewBag.Quote</div>



    


    @using (Html.BeginForm())
    {
        <label style="color:white"> General</label> <input type="radio" name="track" value="everything" />
        <label style="color:white" >Java</label>  <input type="radio" name="track" value="java" />
        <label style="color:white"> .Net</label>  <input type="radio" name="track" value="net" />
        @*<label style="color:white"> javascript</label>  <input type="radio" name="track" value="javascript" />*@
         <input type="submit" value="select resources" />
    }
    <div id="owl-demo">

        @foreach (var carouseldata in (List<CarouselData>)ViewBag.everything)
        {
            <div class="item"><a href="@carouseldata.linkUrl" target="_blank"><img src="@carouseldata.src" alt="Owl Image"></a></div>

        }
      
    </div>

    <script>
        $(document).ready(function () {

            $("#owl-demo").owlCarousel({

                autoPlay: 3000, //Set AutoPlay to 3 seconds

                items: 4,
                itemsDesktop: [1199, 3],
                itemsDesktopSmall: [979, 3]

            });

        });
    </script>
   @*Twitter widget*@
<h2 style="color:white;">Tweet us your programming questions at asg_launchpad</h2>
<a class="twitter-grid" data-limit="3" href="https://twitter.com/ASG_Launchpad/timelines/788378105932951554">New Collection</a>
<script async src="//platform.twitter.com/widgets.js" charset="utf-8"></script>

    

    @*Youtube API*@
    @*1. The <iframe> (and video player) will replace this <div> tag.*@
    <h2 style="color:white;">Feed your mind and body with these helpful videos</h2>
    <div id="player"></div>
    <script>
        // 2. This code loads the IFrame Player API code asynchronously.
        var tag = document.createElement('script');

        tag.src = "https://www.youtube.com/iframe_api";
        var firstScriptTag = document.getElementsByTagName('script')[0];
        firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);

        // 3. This function creates an <iframe> (and YouTube player)
        //    after the API code downloads.
        var player;
        function onYouTubeIframeAPIReady() {
            player = new YT.Player('player', {
                height: '390',
                width: '640',
                playerVars:
                    {
                        listType: 'playlist',
                        // Insert Playlist ID
                        list: 'PLzVFFIhPmg2TOc34Vcsst4ZzTpSGdLaUo'
                    }
            });
        }

        // 4. The API will call this function when the video player is ready.
        function onPlayerReady(event) {
            event.target.playVideo();
        }

        // 5. The API calls this function when the player's state changes.
        //    The function indicates that when playing a video (state=1),
        //    the player should play for six seconds and then stop.
        var done = false;
        function onPlayerStateChange(event) {
            if (event.data == YT.PlayerState.PLAYING && !done) {
                setTimeout(stopVideo, 6000);
                done = true;
            }
        }
        function stopVideo() {
            player.stopVideo();
        }
    </script>





</body>
</html>
